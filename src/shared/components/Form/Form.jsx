"use client";
import React from 'react';
import styled from 'styled-components';

const Form = ({}) => {
	return (
		<FormStl>
 			Form works!
 		</FormStl>
	);
};

export const FormStl = styled.div``;

export default Form;