"use client";
import React from 'react';
import styled from 'styled-components';

const Checkbox = ({}) => {
	return (
		<CheckboxStl>
 			Checkbox works!
 		</CheckboxStl>
	);
};

export const CheckboxStl = styled.div``;

export default Checkbox;