"use client";
import React from 'react';
import styled from 'styled-components';

const ArticleViewer = ({}) => {
	return (
		<ArticleViewerStl>
 			ArticleViewer works!
 		</ArticleViewerStl>
	);
};

export const ArticleViewerStl = styled.div``;

export default ArticleViewer;