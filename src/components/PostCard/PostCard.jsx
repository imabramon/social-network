"use client";
import React from 'react';
import styled from 'styled-components';

const PostCard = ({}) => {
	return (
		<PostCardStl>
 			PostCard works!
 		</PostCardStl>
	);
};

export const PostCardStl = styled.div``;

export default PostCard;